<h2>Create a new account</h2>

<div class="form">

    <%= form_for @user do |u| %>
    <div class="inputs">
        <p>Email<%= u.text_field :email %></p>
        <p>Phone<%= u.text_field :phone %></p>
    <%= u.submit 'Create Account', class: "submit"%>
    </div>

    <% end %>
</div>
